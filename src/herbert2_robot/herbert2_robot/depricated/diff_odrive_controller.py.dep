# ---------------------------------------------------------------

import weakref

from rclpy.node import Node

from .odometry_service import OdometryService
from .node_decorator import NodeDecorator

# ---------------------------------------------------------------
# ---------------------------------------------------------------

class DiffODriveController(NodeDecorator): 
    #class DiffODriveController(Node): 

    def __init__(self, node: Node) -> None:
        self._node_ref = weakref.ref(node)
        self._node_ref().get_logger().info('Initializing Herbert2 ODrive Controller Node') 

        self._odometry = OdometryService(self._node_ref())



# ---------------------------------------------------------------
# ---------------------------------------------------------------


